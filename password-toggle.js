/* Accessible show/hide password control. */
const passwordStyle=document.createElement('style');passwordStyle.textContent=`.password-wrap{position:relative;width:100%}.password-wrap input{padding-right:80px!important}.password-eye{position:absolute;right:7px;top:50%;transform:translateY(-50%);border:0;border-radius:8px;background:var(--soft);color:#2563eb;padding:7px 9px;font:700 11px 'DM Sans';cursor:pointer;letter-spacing:.02em}.password-eye:hover{background:#dbeafe;color:#1d4ed8}.password-eye:focus{outline:3px solid #93c5fd}body.dark .password-eye{background:#172a4c;color:#7dd3fc}body.dark .password-eye:hover{background:#213b69}`;document.head.appendChild(passwordStyle);
function attachPasswordToggles(){document.querySelectorAll('input[type="password"]').forEach(input=>{if(input.closest('.password-wrap'))return;const wrap=document.createElement('div');wrap.className='password-wrap';input.parentNode.insertBefore(wrap,input);wrap.appendChild(input);const button=document.createElement('button');button.type='button';button.className='password-eye';button.setAttribute('aria-label','Show password');button.setAttribute('aria-pressed','false');button.innerHTML='◉ <span>Show</span>';button.onclick=()=>{const visible=input.type==='text';input.type=visible?'password':'text';button.setAttribute('aria-pressed',String(!visible));button.setAttribute('aria-label',visible?'Show password':'Hide password');button.innerHTML=visible?'◉ <span>Show</span>':'◉ <span>Hide</span>'};wrap.appendChild(button)})}
window.addEventListener('hashchange',()=>setTimeout(attachPasswordToggles,30));setTimeout(attachPasswordToggles,30);

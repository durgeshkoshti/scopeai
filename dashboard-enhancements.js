/* Functional navigation, profile editing and interface preferences for the local demo. */
const dashboardLinks = {
  'Overview': '#/dashboard', 'My Project Briefs': '#/dashboard/briefs', 'New Analysis': '#/analyze',
  'Saved Templates': '#/dashboard/templates', 'Settings': '#/dashboard/settings', 'Help Center': '#/dashboard/help',
  'Dashboard': '#/admin', 'Leads': '#/admin/leads', 'Project Briefs': '#/admin/briefs', 'Clients': '#/admin/clients',
  'Analytics': '#/admin/analytics', 'Team': '#/admin/team', 'Logout': '#/'
};
const enhanceCss = document.createElement('style');
enhanceCss.textContent = `
  .page-enter{animation:pageEnter .28s ease-out both}@keyframes pageEnter{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}.motion-off *{animation:none!important;transition:none!important}
  .dash-quick{margin-left:6px}.settings-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}.setting-row{display:flex;justify-content:space-between;align-items:center;padding:15px 0;border-top:1px solid var(--line);gap:14px}.setting-row:first-child{border-top:0}.toggle{appearance:none;width:44px;height:25px;border-radius:99px;border:0;background:#94a3b8;position:relative;cursor:pointer;flex:none}.toggle:after{content:'';width:19px;height:19px;border-radius:50%;background:#fff;position:absolute;top:3px;left:3px;transition:.2s}.toggle:checked{background:var(--blue)}.toggle:checked:after{left:22px}.help-card{border-left:3px solid var(--blue)}.footer-info{display:flex;justify-content:space-between;align-items:center;gap:22px;flex-wrap:wrap}.footer-links{display:flex;gap:16px;flex-wrap:wrap}.footer-links a{margin:0}.dashboard-banner{margin:24px 0 0;padding:20px 22px;border-radius:14px;background:linear-gradient(110deg,#1d4ed8,#7c3aed);color:#fff}.dashboard-banner p{margin:6px 0 0;color:#dbeafe}.dashboard-banner .btn{float:right;background:#fff;color:#1d4ed8}@media(max-width:780px){.settings-grid{grid-template-columns:1fr}.dash-quick{display:none}}
`;
document.head.appendChild(enhanceCss);

function appSettings() { return JSON.parse(localStorage.getItem('scopeai-preferences') || '{"updates":true,"animations":true}'); }
function setAppSetting(key, value) { const prefs = appSettings(); prefs[key] = value; localStorage.setItem('scopeai-preferences', JSON.stringify(prefs)); if (key === 'animations') document.documentElement.classList.toggle('motion-off', !value); toast(value ? 'Animations enabled.' : 'Animations paused.'); }
function settingsView() { const prefs = appSettings(); return `<span class="eyebrow">Account settings</span><h1>Your profile & preferences</h1><p class="muted">Keep your account details and workspace experience up to date.</p><div class="settings-grid"><section class="panel"><h2>Profile details</h2><div class="field"><label for="profileName">Full name</label><input id="profileName" value="${state.name || ''}"></div><div class="field"><label for="profileCompany">Company</label><input id="profileCompany" value="${state.company || ''}"></div><div class="field"><label for="profileEmail">Work email</label><input id="profileEmail" type="email" value="${state.email || ''}"></div><button class="btn" onclick="saveProfileDetails()">Save profile</button></section><section class="panel"><h2>Experience</h2><div class="setting-row"><div><b>Email updates</b><p class="muted">Receive brief and account notifications.</p></div><input aria-label="Toggle email updates" class="toggle" type="checkbox" ${prefs.updates ? 'checked' : ''} onchange="setAppSetting('updates',this.checked)"></div><div class="setting-row"><div><b>Interface animations</b><p class="muted">Use gentle page and card motion.</p></div><input aria-label="Toggle interface animations" class="toggle" type="checkbox" ${prefs.animations ? 'checked' : ''} onchange="setAppSetting('animations',this.checked)"></div><div class="setting-row"><div><b>Theme</b><p class="muted">Choose the light or dark workspace theme.</p></div><button class="btn secondary" onclick="toggleTheme()">Toggle theme</button></div></section></div><section class="panel" style="margin-top:18px"><h2>Website information</h2><p class="muted">ScopeAI helps agencies and product teams turn early client conversations into credible project roadmaps. Contact: <b>hello@scopeai.example</b> · Privacy requests: <b>privacy@scopeai.example</b>.</p></section>`; }
function saveProfileDetails() { state.name = document.getElementById('profileName').value.trim(); state.company = document.getElementById('profileCompany').value.trim(); state.email = document.getElementById('profileEmail').value.trim(); localStorage.setItem('scopeai-state', JSON.stringify(state)); toast('Your profile details have been saved.'); }
function dashboardDetail(path) { const title = path.split('/').pop(); if (title === 'settings') return settingsView(); if (title === 'templates') return `<span class="eyebrow">Saved templates</span><h1>Start from a proven brief.</h1><p class="muted">Reusable discovery templates for your most common project types.</p><div class="cards"><article class="card"><div class="icon">⌁</div><h3>Patient booking platform</h3><p>Appointment booking, reminders, availability and staff operations.</p><button class="btn secondary" onclick="location.hash='#/analyze'">Use template</button></article><article class="card"><div class="icon">▣</div><h3>Customer operations CRM</h3><p>Lead capture, pipeline tracking, reporting and workflow automation.</p><button class="btn secondary" onclick="location.hash='#/analyze'">Use template</button></article></div>`; if (title === 'help') return `<span class="eyebrow">Help center</span><h1>Get more from every brief.</h1><div class="cards"><article class="card help-card"><h3>How does analysis work?</h3><p>ScopeAI turns the business context and constraints you enter into a structured planning document.</p></article><article class="card help-card"><h3>How do I share a brief?</h3><p>Open a brief and select Share public link. You remain in control of when a link is public.</p></article><article class="card help-card"><h3>Need help with your account?</h3><p>Contact hello@scopeai.example for product and account questions.</p></article></div>`; if (title === 'briefs') return `<span class="eyebrow">My project briefs</span><h1>All your project briefs</h1><p class="muted">Review, share and continue conversations from one place.</p><section class="panel"><table class="table"><tr><th>Project</th><th>Type</th><th>Status</th><th>Updated</th><th>Action</th></tr><tr><td><b>${state.brief?.title || 'MedCare Appointment App'}</b></td><td>${state.type || 'Mobile App'}</td><td><span class="badge">In discussion</span></td><td>Today</td><td><a href="#/brief/demo">View →</a></td></tr><tr><td><b>Clinic Website Refresh</b></td><td>Website</td><td><span class="badge should">Draft</span></td><td>Sep 19</td><td><a href="#/analyze">Continue →</a></td></tr></table></section>`; return null; }
function enhancePage() {
  document.querySelectorAll('.side a').forEach(link => { const route = dashboardLinks[link.textContent.trim()]; if (route) link.href = route; });
  document.querySelectorAll('.footer a').forEach(link => { const label = link.textContent.trim(); const route = label === 'Privacy' ? '#/privacy' : label === 'Terms' ? '#/terms' : label === 'Contact' ? '#/contact' : null; if (route) link.href = route; });
  const isHome = (location.hash || '#/') === '#/'; const navLinks = document.querySelector('.navlinks');
  if (isHome && navLinks && !navLinks.querySelector('.dash-quick')) { const quick = document.createElement('a'); quick.className = 'dash-quick'; quick.href = '#/dashboard'; quick.textContent = 'Dashboard'; navLinks.insertBefore(quick, navLinks.querySelector('.signin')); }
  const path = location.hash.slice(1); if (path.startsWith('/dashboard/')) { const detail = dashboardDetail(path); const content = document.querySelector('.content'); if (detail && content) { content.innerHTML = `<div class="page-enter">${detail}</div>`; document.querySelectorAll('.side a').forEach(a => a.classList.toggle('active', a.href.endsWith(location.hash))); } }
  if (path.startsWith('/admin/')) { const content = document.querySelector('.content'); if (content) { const section = path.split('/').pop(); content.innerHTML = `<div class="page-enter"><span class="eyebrow">Agency workspace</span><h1>${section[0].toUpperCase()+section.slice(1)}</h1><p class="muted">Manage your agency’s discovery pipeline and client activity from this workspace.</p><section class="panel"><h2>${section === 'analytics' ? 'Pipeline insights' : 'Workspace information'}</h2><p class="muted">This section is ready for ${section} management. Use the Dashboard menu to return to the agency overview.</p></section></div>`; } }
  document.querySelector('main')?.classList.add('page-enter');
}
if (!appSettings().animations) document.documentElement.classList.add('motion-off');
window.addEventListener('hashchange', () => setTimeout(enhancePage, 0));
setTimeout(enhancePage, 0);
